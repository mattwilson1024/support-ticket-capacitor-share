import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'support-ticket-capacitor-share',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
